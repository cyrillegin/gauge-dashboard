import container from './gaugeContainer.controller';
import gaugeTemplate from './gaugeContainer.template.html';
import './gauge.style.scss';

const gaugeContainer = {
    template: gaugeTemplate,
    controller: container,
};

export default gaugeContainer;
